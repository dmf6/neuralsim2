#include "synapse.h"

class Gap : public Synapse {
  public:
    Gap(Neuron *source, Neuron *target, int type, int iniVarNo);
    void setISyn();
    int derivative(realtype, N_Vector, N_Vector) {
        return 0;
    }
    void setGmax(double gmax);
};

